/// Result of device dot position calculation
class DeviceDotPosition {
  final double angle;
  final double distance;
  final double size;

  const DeviceDotPosition({
    required this.angle,
    required this.distance,
    required this.size,
  });
}