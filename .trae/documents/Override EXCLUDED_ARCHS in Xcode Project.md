I will modify `ios/Runner.xcodeproj/project.pbxproj` to explicitly override the `EXCLUDED_ARCHS` setting.
    *   **Reason:** The file `ios/Flutter/Generated.xcconfig` (generated by Flutter) contains `EXCLUDED_ARCHS[sdk=iphonesimulator*]=i386 arm64`. Your `Runner` target inherits this setting, forcing it to exclude `arm64` even if you remove the setting from the project file.
    *   **Fix:** Explicitly set `EXCLUDED_ARCHS[sdk=iphonesimulator*] = "";` in the `Runner` build configurations (Debug, Release, Profile) in `project.pbxproj`. This overrides the inherited value and allows Xcode to build `arm64` for the simulator.
    *   **Action:** I will insert this setting into the `Debug`, `Release`, and `Profile` configurations for the `Runner` target.